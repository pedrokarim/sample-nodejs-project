# Clé du projet SonarQube (doit correspondre au projet créé dans SonarQube)
sonar.projectKey=sample-nodejs-project

# Nom affiché dans SonarQube
sonar.projectName=sample-nodejs-project

# Répertoires de base
sonar.sources=backend,frontend

# Inclusions strictes : uniquement le code source (le scanner ne parcourt pas node_modules)
sonar.inclusions=backend/server.js,backend/routes/**/*.js,frontend/src/**/*.js,frontend/src/**/*.jsx,frontend/src/**/*.css,frontend/public/**/*.html

# Exclusions de secours
sonar.exclusions=**/node_modules/**,**/coverage/**,**/build/**,**/dist/**,**/*.test.js,**/*.spec.js

# Rapports de couverture (optionnel)
# sonar.javascript.lcov.reportPaths=backend/coverage/lcov.info,frontend/coverage/lcov.info
